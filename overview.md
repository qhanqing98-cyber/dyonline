# 前端重写总览 — DY·ONLINE 视频下载台

## 交付物
| 文件 | 变更 | 说明 |
|------|------|------|
| `templates/index.html` | 重写 | 页面骨架：顶栏(品牌+统计)、输入区、过滤栏、任务列表、空状态、toast 容器 |
| `static/style.css` | 重写 | 暗色「下载指挥舱」主题，~600 行，CSS 变量 + 响应式 + 动效 |
| `static/app.js` | 新增 | 全部交互逻辑：识别/提交/轮询/渲染/通知 |

后端零改动，API 契约完全复用。

## 设计
- 品牌色：抖音青 `#25f4ee` × 抖音红 `#fe2c55` 渐变（呼应两个数据源平台）
- 等宽字体（Cascadia Code / Consolas）用于 ID、进度、日志 → 终端质感
- 背景双层氛围：双色径向辉光 + 顶部渐隐网格
- 状态色条：任务卡片左侧 3px 色条（琥珀=排队 / 青=下载 / 绿=完成 / 红=失败）

## 交互清单
1. 刷新不丢任务：加载时 `GET /api/tasks` 恢复历史
2. 输入实时识别：逐行正则判断抖音/B站，徽章计数
3. 提交：按钮 loading 态 + toast 反馈 + Ctrl+Enter 快捷键
4. 轮询：1.5s，仅活跃任务，并行请求，页面隐藏暂停，fingerprint 去重渲染
5. 日志：默认折叠，点击展开，自动滚底，[OK]/[x] 着色
6. 过滤：全部/进行中/已完成/失败 + 「隐藏已结束」
7. 通知系统替代 alert
8. XSS：所有动态内容 escapeHtml 转义

## 响应式（680px 断点）
- 统计条横向滚动、操作按钮全宽 46px 触控高度、过滤栏取消 sticky、toast 移至底部

## 验证
- 本机 8000 端口已有运行中的 uvicorn，已直接热加载新前端（curl 200 确认新模板与 JS 生效）
- 浏览器若显示旧样式，Ctrl+F5 强刷即可
